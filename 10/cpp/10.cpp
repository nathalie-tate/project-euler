#include<cstdio>
#include<ctime>

bool isPrime(int);

int main()
{
  std::time_t start = std::time(0);

  int sum = 5;
  int currentPrime = 3;

  for (int i = 5; currentPrime < 2000000; i++)
  {
    if(isPrime
  }

  std::time_t end = std::time(0);
  printf("Time: %d\n", end - start);
  return(0);
}
